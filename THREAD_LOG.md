# Thread Log

Append short handoff entries here. Durable findings belong in `spec/`, not only in this log.

## Entries

- 2026-05-01: Packet created from the side-projects backlog plan. Current stage is discovery; next work is to refine the orchestration concept and recommend a minimum useful first version.
- 2026-05-01: Inspiration review session. Read sandcastle/README.md, symphony/SPEC.md (with elixir/WORKFLOW.md and .codex/skills/linear/SKILL.md as concrete examples), paperclip/README.md, openclaw/README.md, hermes-agent/README.md, and graphify/README.md. Captured durable notes in spec/inspiration-review.md and a synthesized direction in spec/discovery.md. Resolved three open questions: runner-agnostic at the packet layer with Sandcastle Docker as the first concrete runner; orchestrator stays inside this repo until a daemon ships; build-readiness gate is a fully filled spec/build-spec.md plus empty open_questions plus a resolved EXTRACT.md. Six new open questions opened around WORKFLOW.md schema/location, prompt template language, the v1 packet picker, when to run graphify, and how to share skills across packets. Next session should draft the WORKFLOW.md template and a graphify integration plan in spec/build-spec.md.
- 2026-05-01: Architectural decisions session. User confirmed: (1) pipeline.yaml-as-engine is dropped because we are building real software, replaced by typed Pydantic models and a code-defined state machine; the yaml file stays as informational text only. (2) Jinja2 with StrictUndefined over plain {{KEY}} substitution because conditionals are real (first-run vs retry-run prompts) and strict mode turns prompt typos into test failures. (3) Python over TypeScript as the implementation language, primarily because Graphify is Python and is an explicit memory layer in the plan, the LLM/agent ecosystem is most mature in Python, FastAPI gives the cleanest eventual SaaS surface, and Sandcastle as a subprocess is a clean boundary. Stack: Python 3.12 + uv, Pydantic v2, Jinja2 strict, Typer CLI, ruamel.yaml for round-trip, SQLite for v1 run history, FastAPI + arq + Postgres deferred to V2, multi-tenancy deferred to V3.
- 2026-05-01: Build spec filled. spec/build-spec.md now contains Outcome, User Workflows (A-G), Implementation Direction (architecture layers, atomicity, storage, Sandcastle as subprocess), Interfaces and Data (Stage/Phase enums, PacketStatus/WorkflowDefinition models, predicate vocabulary, runner protocol, full CLI surface, WORKFLOW.md example, run record schema), Acceptance Criteria (18 items), Test Plan (unit/integration/manual smoke/CI), Extraction Notes, V2/V3 outline, and Open Items Resolved. EXTRACT.md updated with extraction target details and pre-extraction items checked. status.yaml stage promoted from discovery to spec_ready; phase_status.build_spec set to complete; all 9 outstanding open_questions resolved and moved to resolved_questions; open_questions list is now empty.
- 2026-05-01: Project renamed from working title "Sandcastle-like Agent Orchestration" to **SwarmLord** (user-chosen, after brainstorming). The two halves of the name carry function the way Sandcastle does: "swarm" = many parallel agent-workers, "lord" = the singular orchestrator coordinating them. Renamed throughout: README.md (H1, summary, intended outcome, next-agent-task), GUIDE.md (title), workflow/status.yaml (project_name and several owner_notes / next_actions / resolved_questions), projects/INDEX.md (row name and summary), spec/build-spec.md (project-name header, all `orch ...` CLI references flipped to `swarmlord ...`, src/orch/ module path flipped to src/swarmlord/, runs.db path updated, Extraction Notes naming section rewritten, Open Items Resolved naming entry rewritten), EXTRACT.md (Extraction Target section). Folder slug `2026-05-sandcastle-like-agent-orchestration` retained as historical trace of the originating idea. The extracted repo is named `swarmlord` at `~/Documents/GitHub/swarmlord/`. CLI binary and Python package are both `swarmlord` (with `swarm` available as an alias if desired). Next agent should treat this packet as build-ready input, extract to ~/Documents/GitHub/swarmlord/, and implement v1 from spec/build-spec.md without re-deciding architecture or naming.
- 2026-05-01: Domain `swarmlord.dev` purchased by user. Brand is locked. README naming note, build-spec project-name header, build-spec V2/V3 outline (added a suggested subdomain plan: marketing site at swarmlord.dev, app at app.swarmlord.dev, api at api.swarmlord.dev, docs at docs.swarmlord.dev, tenant routes via subdomain or path — final shape decided at V3 design time), build-spec Open Items Resolved (name now marked locked), EXTRACT.md Extraction Target (domain recorded; PyPI fallback to `swarmlord-orchestrator` if `swarmlord` is taken since the domain is now the source of brand truth, not PyPI), and status.yaml owner_notes all updated to reflect ownership.
- 2026-05-01: This repo extracted from side-projects/projects/2026-05-sandcastle-like-agent-orchestration/. Initial copy done from a sandboxed environment that could not finalize git init due to bind-mount permissions; user cleaned the stale .git/.git.broken artifacts on Windows, ran git init + initial commit, scrubbed all 40k/Tyranid/Hive-Mind IP references with `git commit --amend`, and pushed to GitHub at TheAmericanMaker/swarmlord. Optional self-hosted-git mirror was discussed (Gitea/Forgejo on the LAN at port 2222 via multi-URL push on `origin`) but not yet wired.
- 2026-05-01: AGENTS.md Setup section refreshed. Removed the stale rmdir/git-init/.git.broken instructions; replaced with the current "start v1 here" flow: uv init --package, uv add for v1 dependencies (pydantic, typer, jinja2, ruamel.yaml, python-frontmatter, rich, httpx; pytest/mypy/ruff as dev), pyproject.toml [project.scripts] entry for swarmlord and `swarm` alias, plus an optional "mirror to a self-hosted git server" subsection documenting the multi-URL push pattern. README.md Status flipped from "spec-ready, awaiting v1" to "pre-v1, repo live on GitHub, no code yet"; "What this repo contains" now lists AGENTS.md first; "Implementation entry point for the next agent" points at AGENTS.md instead of GUIDE.md.
- 2026-05-01: V1 implementation landed. Scaffolded uv project (Python 3.12) with deps from build-spec (pydantic v2, typer, jinja2 strict, ruamel.yaml, python-frontmatter, rich, httpx, pyperclip; dev: pytest+cov+asyncio+mock, mypy, ruff, fastapi). Implemented full architecture: `core/` (Pydantic models, Stage/Phase StrEnums, transition table, predicate evaluators with FileExists/FileSectionFilled/YamlFieldEmpty/YamlFieldEquals/ExtractMdResolved/TestsPassing, typed errors); `packets/` (discovery walker, ruamel.yaml round-trip reader, atomic writer with tempfile+os.replace, append-only THREAD_LOG, INDEX.md upsert); `templating/` (Jinja2 with StrictUndefined + autoescape=False + custom filters trim/indent/default_empty/summarize, user-supplied content treated literally); `runners/` (Runner Protocol + RunRequest/RunResult, ManualRunner with clipboard fallback, ClaudeCodeInteractiveRunner subprocess, SandcastleDockerRunner that generates .sandcastle/main.ts+prompt.md and parses __SANDCASTLE_SUMMARY__ JSON line, RunnerRegistry); `memory/` (graphify subprocess wrapper); `storage/` (SQLite at ~/.local/share/swarmlord/runs.db or %APPDATA% on Windows, three tables: runs / gate_evaluations / transitions); `service.py` (orchestration glue: list/pick/new/render/promote/dispatch/extract/validate); `cli.py` (Typer app with all 11 commands from spec); `server/` (FastAPI 501 stubs ready for V2). Bundled packet template in `src/swarmlord/_templates/packet/` with a workflow/WORKFLOW.md replacing the old pipeline.yaml engine; repo-local `templates/packet/` updated to the new schema and pipeline.yaml removed. 78 tests across schema, transitions, gate evaluators, workflow parsing, templating (including StrictUndefined typo and literal `{{` preservation), packet I/O (including atomic-write failure simulation), manual + sandcastle runners (subprocess injected for tests), storage round-trips, service orchestration, CLI via Typer's CliRunner, and two integration tests (full idea→spec_ready cycle, multi-packet pick_next ordering). All pass; coverage 85% overall, all required layers >=80%. `mypy --strict`, `ruff check`, `ruff format --check`, and `pytest --cov` all green. CI workflow at `.github/workflows/ci.yml` runs the full gate on push/PR. Acceptance criteria 1-18 from spec/build-spec.md are all addressed; the only items deliberately uncovered in unit tests are real-binary smoke tests for claude-code-interactive and sandcastle-docker (covered behind subprocess mocks; manual smoke tests remain the acceptance path per the spec). Next agent: dogfood `swarmlord list / render / promote` against side-projects/projects/2026-05-sandcastle-like-agent-orchestration/, then start V2 server bodies.

- 2026-05-01: V1.0.1 follow-ups landed after the architecture review. (1) Removed the silent gate-default fallback in service.promote — when WORKFLOW.md exists, its gates list is authoritative even if empty; defaults only apply when no WORKFLOW.md is present. (2) Replaced the brittle slug-prefix heuristic in new_packet with a `^\d{4}-\d{2}-` regex; added tests for already-prefixed slugs, unprefixed slugs, and edge cases like `12-1234-foo` that fooled the old check. (3) Consolidated the default WorkflowDefinition into a single `default_workflow_definition()` helper used by both the bundled template path and the synthetic-workflow path inside dispatch_run. (4) Extracted `resolve_runner_profile(bundle, override, fallback="manual")` with a documented precedence chain (override > status pin > workflow > fallback). (5) Wired PHASE_FOR_STAGE into a documented `resolve_phase()` helper. (6) Manual runner now takes a `warn_writer` callback so clipboard failures surface visibly; CLI run_cmd builds a CLI-specific RunnerRegistry that pipes warnings to err_console as `[yellow]…[/yellow]`. (7) FastAPI router construction moved fully inside `create_app()`; the `try/except ImportError` dance in each `server/api/*.py` is gone — they expose `build_router()` instead. (8) Added `TODO(v1-smoke)` block at the top of the Sandcastle runner flagging the unverified `@anthropic/sandcastle` package import that needs validation on first real integration smoke test.
- 2026-05-01: Four P-items from James's static review fixed forward. (P1) dispatch_run now accepts an optional `history: RunHistory | None` and persists the RunRecord on both the success and exception paths — runner crashes and Sandcastle summary-parse failures now leave a `status="failed"` audit row in SQLite with the exception text in `error`. CLI run_cmd passes a `RunHistory()`. (P2 commits) Sandcastle commits returned on RunResult are now copied into RunRecord on the success-path model_copy and persisted. (P2 extract) extract_packet calls `assert_transition(stage, EXTRACTED)` and evaluates the `promote_to_extracted` predicates by default; pass `force=True` (or `--force` on CLI) to bypass for emergency extraction of a half-finished packet. (P3) Added `_confine_path(root, rel)` helper in core/gates.py that resolves and verifies the candidate path stays inside the packet root; FileExists, FileSectionFilled, and the YAML predicates all use it now, returning a clean `escapes the packet root` GateResult instead of letting `..` segments read arbitrary disk. Tests cover all four: failed-record persistence with a CrashingRunner, commits round-trip with a CommittingRunner, three new extract paths (idea→extracted rejection, --force bypass, build_ready→extracted with passing gates, build_ready→extracted with unresolved EXTRACT.md), and three path-confinement tests (traversal blocked, in-root allowed, YAML predicate also blocked). Final state: 94 tests passing, 85% overall coverage with service.py at 91%, all four CI gates (ruff check, ruff format --check, mypy --strict, pytest --cov) green.

- 2026-05-01: Dogfood loop succeeded. Ran `swarmlord validate --all`, `swarmlord list`, and `swarmlord render 2026-05-sandcastle-like-agent-orchestration` from the side-projects repo against the originating packet. First pass surfaced two schema gaps: the originating packet had `extracted_to` and `extracted_on` fields that the new schema didn't define, and one of the `owner_notes` items wasn't a string. Fixed both forward (per spec rule "drops nothing") rather than migrating the on-disk data: added `extracted_to: str | None` and `extracted_on: date | None` to PacketStatus, and added a `before` field validator that coerces non-string items in `next_actions`/`assumptions`/`open_questions`/`resolved_questions`/`owner_notes` to strings (None→"", numbers→str, dicts→repr) so realistic packets remain readable. Also fixed the related UX bug where `swarmlord list` silently dropped invalid packets: added `discover_failures()` in `packets/discovery.py` and wired the CLI to print a yellow `invalid <slug>: <error>` line after the table when any packet fails schema; `resolve_packet()` now points the user at `swarmlord validate <slug>` when a packet exists on disk but is invalid. Three new tests cover the schema additions; existing 94 tests still pass for a total of 97. Acceptance Criteria #1 (`list` enumerates packets), #2 (`validate --all` passes against existing packets), and #4 (`render` produces a deterministic prompt) are now satisfied against real data, not just fixtures. Next moves: `swarmlord log <slug>` to read run history back, deterministic-render byte-equality test, CHANGELOG.md.

- 2026-05-01: V0.1.1 polish wave landed (the three pre-V2 follow-ups). (1) Added `swarmlord log <slug>` reading runs + gate evaluations + transitions back from SQLite via a new `RunHistory.list_gate_evaluations()` method; CLI flags are `--limit`, `--gates`, `--transitions`, `--json`. Smoke-tested end-to-end: `swarmlord run` records a row, `log` shows it; `swarmlord promote` records a transition, `log --transitions` surfaces it. (2) Added `CHANGELOG.md` in keepachangelog.com format with 0.1.0 (V1 land) and 0.1.1 (V1.0.1 fixes + P-item review + dogfood compatibility) sections; bumped pyproject.toml and `__version__` to 0.1.1. (3) Added a deterministic-render byte-equality test in test_templating.py that satisfies Acceptance Criterion #4 with a regression net — catches accidental injection of timestamps, random IDs, or anything that would silently make rendered prompts non-reproducible. Also added a basic `swarmlord log` CLI test for the no-history path. Final state: 99 tests passing, 85% coverage, all four CI gates green (ruff check, ruff format --check, mypy --strict, pytest --cov). V1 is complete and dogfooded; V2 work (FastAPI route bodies, arq workers, Postgres) can begin from a clean baseline.

- 2026-08-26: Scope pivot — SwarmLord is now a local, single-user MIT open-source project with no hosted phase. User direction: "there will be no SaaS phase for this," repositioned as a portfolio project. (1) Deleted `src/swarmlord/server/` (the FastAPI 501 scaffold), the `swarmlord serve` command, `test_serve_is_v2_stub`, the `[project.optional-dependencies] server` group (fastapi, uvicorn), `fastapi` from the dev group, and the `server/*` coverage omit. Suite is 98 tests; coverage held at 84.86% with the omit gone, so removing the scaffold cost nothing. (2) README rewritten for a general open-source audience — leads with the problem and the gate model, shows real captured command output including a failing promotion with its exit code, documents the four quality gates in a table, adds an explicit Scope section stating the local-only boundary. Dropped the PyPI badge (package is not published; 404) in favor of `uv tool install git+…`, and dropped the v1/v2/v3 roadmap and `swarmlord.dev` product framing. (3) All docs converted from PowerShell to POSIX shell (9 blocks in GUIDE.md alone); machine-specific paths (`~\Documents\GitHub\...`, `C:\Users\Inven\...`) genericized. (4) `spec/` relabelled a historical design record: each of the four files carries a banner, the "V2 and V3 Outline" section is replaced by a "Later Phases — Dropped" note, and the server module is gone from the package tree and CLI surface listings. The one salvageable idea from that outline — evaluating `tests_passing` against an ephemeral worktree — is carried forward into AGENTS.md "What's open". (5) AGENTS.md rewritten with a Scope section; CONTRIBUTING/SECURITY/CLAUDE.md/feature_request.yml updated to match. (6) Added a `Makefile`: `make check` runs all four gates, plus `install`/`fmt`/`cov`/`clean`. (7) Deleted a 45 KB terminal transcript accidentally committed at the repo root (it contained a personal email address). Release workflow left intact and ready for a future tag. All four gates green.
