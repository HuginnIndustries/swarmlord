# Packet Skills

Per-phase prompt fragments. The packet's `workflow/WORKFLOW.md` template can include these explicitly via Jinja, or the runner can copy them into the agent's working context as needed.
